; While loop test
(nya loop_test
  (nyan ((i int) (sum int)))
  (paw
    (:= i 0)
    (:= sum 0)
    (purr-while (<= i 5)
      (paw
        (:= sum (+ sum i))
        (:= i (+ i 1))))
    (meow sum)))
