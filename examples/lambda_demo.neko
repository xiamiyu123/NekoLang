; lambda_demo - Lambda expression examples
; 展示匿名函数、函数类型和高阶函数的基本写法。
(nya lambda_demo
  (nyan ((double (func (int) int))
         (add (func (int int) int))
         (f (func (int) int))
         (result int)))
  (paw
    ; 把一个 lambda 赋给变量，再传给高阶函数调用。
    (:= double (lambda ((n int)) int (return (* n 2))))
    (:= result (apply double 5))
    (meow result)

    ; 多参数 lambda 也可以像普通函数一样传递。
    (:= add (lambda ((a int) (b int)) int (return (+ a b))))
    (:= result (apply2 add 3 4))
    (meow result)

    ; 命名函数也能作为值保存到函数类型变量里。
    (:= f square)
    (:= result (apply f 6))
    (meow result)

    ; 接收单参数函数的高阶函数。
    (function apply ((g (func (int) int)) (x int)) int
      (return (g x)))

    ; 接收双参数函数的高阶函数。
    (function apply2 ((g (func (int int) int)) (a int) (b int)) int
      (return (g a b)))

    ; 普通命名函数，可被直接调用，也可作为值传递。
    (function square ((n int)) int
      (return (* n n)))))
