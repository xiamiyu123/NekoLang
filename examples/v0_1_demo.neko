; NekoLang v0.1 demo
; Computes 1 + 2 + 3 + 4 + 5, then checks whether the result is positive.
(nya v0_1_demo
  (nyan ((n int) (i int) (total int) (flag bool)))
  (paw
    (nyaa-def add ((a int) (b int)) int
      (return (+ a b)))

    (nyaa-def positive ((x int)) bool
      (return (> x 0)))

    (:= n 5)
    (:= i 1)
    (:= total 0)

    (purr-while (<= i n)
      (paw
        (:= total (add total i))
        (:= i (+ i 1))))

    (:= flag (positive total))
    (meow total)
    (meow flag)))
